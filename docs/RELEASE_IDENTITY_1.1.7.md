# ApexTuner 1.1.7 — release identity

Current Android release identity:

- `versionName`: `1.1.7`
- `versionCode`: `44`
- application ID: `com.apextuner.app`

This note records the current release metadata only. Historical audit documents that mention earlier versionCode builds are retained as historical records and do not define the current APK/AAB identity.
